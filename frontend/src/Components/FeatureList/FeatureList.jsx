// this components contains the div which has all the features in a grid manner
// each feature is a rectangular box with feature name and button

import React from 'react'

const FeatureList = () => {
  return (
    <div className='feature-list'>
      
    </div>
  )
}

export default FeatureList
